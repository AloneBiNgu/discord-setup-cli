# discord-setup-cli

`discord-setup-cli` is a command-line tool to quickly set up a Discord.js bot project. This tool copies the necessary files and folder structure from a template to your working directory, allowing you to start building your Discord bot without worrying about setup.

## Features

- Easily set up the structure for a Discord.js bot project.
- Copy predefined files and directories to your project folder.
- Fast and simple setup with just one command.

## Installation

### Global Installation

To install `discord-setup-cli` globally, run the following command:

```bash
npm install -g discord-setup-cli
